export enum SumType {
  Default = 'default',
  ColTotal = 'colTotal',
  RowTotal = 'rowTotal',
  SubTotal = 'subTotal',
  RowSubTotal = 'rowSubTotal',
  ColSubTotal = 'colSubTotal'
}

export enum CategoryType {
  Row = 'row',
  Col = 'col',
  Metrics = 'metrics'
}

export enum SumSuffixType {
  Sum = 'sum',
  SubSum = 'subSum'
}
